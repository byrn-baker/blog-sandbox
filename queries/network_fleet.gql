query NetworkTelemetryFleet {
  devices {
    name
    role { name }
    platform { name }
    secrets_group { name }
    primary_ip4 { address }
    location { name }
    config_context
    interfaces { name vrf { name } }
  }
}
