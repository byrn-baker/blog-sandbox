{# VRF instances modeled on this device #}
{% set seen_vrfs = [] %}
{% for interface in interfaces if interface.vrf %}
{% if interface.vrf.name not in seen_vrfs %}
{% if seen_vrfs.append(interface.vrf.name) %}{% endif %}
vrf instance {{ interface.vrf.name }}
!
{% endif %}
{% endfor %}
