{# MPLS LDP configuration — driven by config_context.mpls #}
{% if igp_routing_instances and config_context.mpls is defined %}
mpls ldp router-id {{ config_context.mpls.ldp_router_id }} force
{% if config_context.mpls.explicit_null | default(false) %}
mpls ldp explicit-null
{% endif %}
!
{% endif %}
