---
# Local config context for DCB-k3s-w1 (K3s agent + Longhorn storage node, DC-B).
k3s:
  member: true
  role: "agent"
  node_labels: "topology.kubernetes.io/zone=dc-b"
storage:
  enabled: true
  disk_device: "/dev/sdb"
  mount_path: "/var/lib/longhorn"
