---
# Ansible collections needed by the Part 6 playbooks.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: networktocode.nautobot
    version: ">=6.0.0,<7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=8.0.0"
